apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "transfer-service.fullname" . }}-monitor
  labels:
    app: {{ include "transfer-service.name" . }}
    release: {{ .Release.Name }}
spec:
  jobLabel: transfer-service
  endpoints:
    - port: actuator
      path: /actuator/prometheus
      interval: 10s
  selector:
    matchLabels:
      app: {{ include "transfer-service.name" . }}


