apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "block-service.fullname" . }}
  labels:
    app: {{ include "block-service.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "block-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "block-service.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.ports.block_service_port }}
              name: app
            - containerPort: {{ .Values.livenessProbe.httpGet.port }}
              name: actuator
          livenessProbe:
            httpGet:
              port: 7086
              path: actuator/health
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              port: 7086
              path: actuator/health
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: {{ include "block-service.name" . }}
              mountPath: /config
          envFrom:
            - configMapRef:
                name: {{ include "block-service.fullname" . }}-env
      volumes:
        - name: {{ include "block-service.name" . }}
          configMap:
            name: {{ include "block-service.fullname" . }}-config