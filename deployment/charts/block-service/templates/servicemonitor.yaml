apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "block-service.fullname" . }}-monitor
  labels:
    app: {{ include "block-service.name" . }}
    release: {{ .Release.Name }}
spec:
  jobLabel: block-service
  endpoints:
    - port: actuator
      path: /actuator/prometheus
      interval: 10s
  selector:
    matchLabels:
      app: {{ include "block-service.name" . }}


