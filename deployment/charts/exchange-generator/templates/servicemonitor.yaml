apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "exchange-generator.fullname" . }}-monitor
  labels:
    app: {{ include "exchange-generator.name" . }}
    release: {{ .Release.Name }}
spec:
  jobLabel: exchange-generator
  endpoints:
    - port: actuator
      path: /actuator/prometheus
      interval: 10s
  selector:
    matchLabels:
      app: {{ include "exchange-generator.name" . }}


