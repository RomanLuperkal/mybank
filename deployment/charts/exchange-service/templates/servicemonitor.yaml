apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "exchange-service.fullname" . }}-monitor
  labels:
    app: {{ include "exchange-service.name" . }}
spec:
  jobLabel: exchange-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 10s
  selector:
    matchLabels:
      app: {{ include "exchange-service.name" . }}


