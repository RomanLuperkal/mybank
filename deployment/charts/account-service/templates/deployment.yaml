apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "account-service.fullname" . }}
  labels:
    app: {{ include "account-service.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "account-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "account-service.name" . }}
    spec:
      initContainers:
        - name: wait-for-postgres
          image: postgres:16
          command: [ 'sh', '-c', 'until pg_isready -h postgres-0.postgres.{{ .Release.Namespace }}.svc.cluster.local -p 5432; do echo waiting for postgres; sleep 2; done;' ]
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.ports.account_service_port }}
              name: app
            - containerPort: {{ .Values.livenessProbe.httpGet.port }}
              name: actuator
          livenessProbe:
            httpGet:
              port: 7081
              path: actuator/health
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              port: 7081
              path: actuator/health
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: {{ include "account-service.name" . }}
              mountPath: /config
          envFrom:
            - configMapRef:
                name: {{ include "account-service.fullname" . }}-env
          env:
            - name: ACCOUNT_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: keycloak-sc
                  key: ACCOUNT_CLIENT_ID
            - name: ACCOUNT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-sc
                  key: ACCOUNT_SECRET
            - name: ACCOUNT_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-sc
                  key: POSTGRES_USER
            - name: ACCOUNT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-sc
                  key: POSTGRES_PASSWORD
      volumes:
        - name: {{ include "account-service.name" . }}
          configMap:
            name: {{ include "account-service.fullname" . }}-config