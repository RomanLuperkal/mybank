apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "cash-service.fullname" . }}-monitor
  labels:
    app: {{ include "cash-service.name" . }}
spec:
  jobLabel: cash-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 10s
  selector:
    matchLabels:
      app: {{ include "cash-service.name" . }}


